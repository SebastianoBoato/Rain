<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.Rain" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here -->
        <item name="android:fontFamily">@font/helvetica</item>
    </style>

    <style name="Theme.Rain" parent="Base.Theme.Rain" />

    <style name="App.No.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@android:color/transparent</item>
    </style>

    <style name="EmptyFont" />
</resources>
